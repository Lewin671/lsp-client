
## Objective

Design a set of LSP Client Agent architecture that is independent of VS Code yet conceptually references vscode-language-client, enabling the reuse of a single LSP client implementation across arbitrary host environments (CLI / GUI / Web / self-developed IDEs).
